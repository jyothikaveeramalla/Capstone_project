# Copy this file to .env and fill in your values
# Do NOT commit .env to Git (it's in .gitignore)

# === DATABASE_URL ===
# For Docker Postgres (Option A - Recommended for teams on same network):
# Run scripts/start_docker_db.ps1 on the host, then use the DATABASE_URL it provides
DATABASE_URL=postgres://artisan:artisanpw@127.0.0.1:5432/artisandb

# For Railway Postgres (Option B - For distributed teams):
# Get this from Railway dashboard > Postgres plugin > Connect tab
# DATABASE_URL=postgres://postgres.xxxxxxx_user:xxxxxxxxxxxx@gateway.railway.app:5432/railway

# For local SQLite (fallback if no DATABASE_URL set):
# (leave DATABASE_URL empty or commented out above)

# === SECRET KEY ===
# Change this to a long random string for production
# Keep it secret; do not share or commit
DJANGO_SECRET_KEY=your-secret-key-here-minimum-50-chars

# === DEBUG ===
# Set to False in production
DEBUG=True
